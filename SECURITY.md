# NullEye Security

Principles

- Least privilege for runtime components.
- Defense in depth: kernel telemetry is read-only; actions require explicit rules.

Reporting

Report vulnerabilities via the repository's security contact. Do not disclose publicly until a fix is available.
